# Observe/Tune Log

## 2026-06-12 01:10 UTC

- Ran post-fresh health check after recent DB/tooling/Gitea changes.
- Fixed stale Makefile database assumptions so fresh/migrate/nuke/engage targets use `.env` `DATABASE_URL` instead of hardcoded `subcorp_ops/subcorp`.
- Added reusable startup helpers: `scripts/go-live/reset-db-from-env.mjs`, `scripts/go-live/migrate.mjs`, and `scripts/go-live/set-system-enabled.mjs`.
- Made `docker/toolbox/init-workspace.sh` safer to rerun and hot-patched the live toolbox with updated Gitea-safe scripts.
- Re-ran migrations, seed, and workspace init against the actual app DB (`subcult_ops`); seed counts are healthy and `system_enabled` is true.
- Verified heartbeat manually and via worker logs. Heartbeat is active and returning `status: ok`.
- Tests: `bun test` passed, `tests/agent-tooling-gitea-regression.test.ts` passed, `npx tsc --noEmit` passed.
- Observed real tool use and artifact writes via `qwen3:14b`; recent artifacts are concrete. Some steps are blocked by required-tool-evidence gates, which is expected after the tooling fix but should be monitored for excessive false blocking.
- Next loop: watch blocked step rate, confirm code-writing missions get `file_write` evidence, and consider lowering noisy audit trigger volume if it keeps producing false-positive container findings.


## 2026-06-12 01:44 UTC — observe/tune cycle

- Startup/fresh: did not run destructive fresh; not needed. Re-ran `prod-migrate`, `prod-seed`, `prod-init-workspace`, and `prod-engage` idempotently. Seed state: agents=6, policies=36, triggers=31, enabled_triggers=30, rss_feeds=15, discord_channels=16, cron schedules=12/12 enabled, `system_enabled={"enabled": true}`.
- Containers: `subcorp-app`, `subcorp-worker`, `subcorp-sanctum`, `subcorp-toolbox` running with restart count 0 after rebuilds. Toolbox remote remains tokenless Gitea `https://git.subcult.tv/subculture-collective/subcorp.git`.
- Heartbeat: worker-managed heartbeat is active and returning `status: ok`; recent cycles evaluated 30 triggers and 12 cron schedules.
- Findings: high blocked-step rate was correlated with local qwen3 tool sessions exceeding the previous 90s total LLM tool budget before final text/artifact completion. Increased `LLM_TOOL_TOTAL_BUDGET_MS` to 240s and rebuilt app/worker.
- Findings: workspace files were being created world-writable (`0666`) by direct workspace writers. Patched `file_write`, `send_to_agent`, droid task setup, project registry initialization, newspaper/newsletter writes, and local blog publication writes to normalize files to `0644`; normalized existing `/workspace` files.
- Validation: `bun test tests/agent-tooling-gitea-regression.test.ts` passed (9 tests), `bun test` passed (51 tests), `npx tsc --noEmit` passed, `rtk lint` had 0 errors and the same 7 pre-existing unused-var warnings in `step-prompts.ts`/`triggers.ts`. Next.js production build and worker bundle rebuilt successfully.
- Post-fix observation: tool sessions now continue longer through multi-round `web_search`, `web_fetch`, and `file_read` work instead of immediately exhausting budget. Some Brave Search rate limiting persists and falls back to DuckDuckGo. One remaining `0666` inbox artifact was older than the final rebuild window and was normalized; next loop should verify no new `0666` files appear.
- Next loop watch items: blocked-session rate after 240s budget, any new world-writable files, rate-limit impact on research quality, and whether running tool sessions convert to succeeded artifacts rather than blocked steps.


## 2026-06-13 — next-wave stabilization

- Fixed toolbox command contract so agents can use `/usr/local/bin/sync-workspace-to-gitea.sh` without PATH ambiguity.
- Added host/container audit scope labelling so operational audits cannot mistake toolbox-local checks for host-level exposure checks.
- Updated workspace permission hygiene to normalize world-writable files after detection and verify clean state after correction.
- Added counters/provenance for empty LLM tool rounds and web-search provider fallback so noisy recovery paths are measurable instead of anecdotal.
- Validation: `bun test` passed (71 tests), `rtk tsc --noEmit` passed, `rtk lint` passed, `make prod-heartbeat` returned `status: ok`, Subcorp containers were up, and the `/workspace` world-writable scan returned no files.
- Watch next: qwen3 empty tool-round rate, Brave fallback frequency, stale session sweeps, and whether host-level audit snapshots reduce false-positive/false-negative audit reports.


## 2026-06-13 — observe loop baseline after review/path gate

- Runtime: `subcorp-app`, `subcorp-worker`, `subcorp-toolbox`, and `subcorp-sanctum` were up about one hour after deploy. Git was clean at `1f7f2d1 fix: gate review packets and ground workspace paths`.
- Heartbeat: manual `prod-heartbeat` returned `status: ok` but was slow (`111426ms`). It evaluated 30 triggers, fired 0 in the manual response, fired 1 cron schedule, learned 2 memories, and enqueued one roundtable. Artifact counts today reached 110 manifest entries with synthesis success at 88% and 2 blocked synthesis sessions.
- Review gate: the new content-review preflight is active. App logs show it rejected at least two drafts for missing opening metadata/reviewer context instead of queueing weak review sessions. This looks correct, but next loops should ensure authors learn to include metadata rather than creating a growing `needs_revision` backlog.
- Output quality: recent product/spec docs are concrete enough to read, but still generic. Latest audit output was still weak: it made claims about missing `/output/reports/` and unreadable inbox content without the required evidence table, so audit quality should remain on the watchlist until fresh post-gate audit sessions complete or block.
- LLM/tool symptoms in the recent worker log sample: 7 `Ollama model returned empty text`, 7 `consecutiveEmptyRounds=2`, 17 `File read failed`, 2 references to `/workspace/src`, 8 references to `/workspace/projects/src`, 4 references to `/workspace/workspace`, 2 Brave rate-limit fallbacks, and 0 `agent_session_blocked` strings in the sampled worker log. Empty rounds remain noisy but mostly recover.
- Path regression: agents are still treating `/workspace/projects` as a concrete repo and writing/reading `/workspace/projects/src/*`. The correct contract should be stricter: product code must live under `/workspace/projects/<project-slug>/...`; `/workspace/projects/subcorp` is the platform checkout; `/workspace/projects/src` is root-level workspace drift, not a managed product repo.
- Workspace hygiene: permission scan returned clean. Nested git repos still exist under `/workspace/projects/*/.git`; no immediate runtime breakage observed. The generated hygiene file was not present in this sample, likely because the registry refresh path has not run since the new code deployed.
- Watch next loop: heartbeat duration, `needs_revision` backlog size/reasons, whether new audits block without evidence instead of writing unsupported findings, recurrence of `/workspace/projects/src` writes, qwen empty-round trend, and whether `workspace-hygiene.json` appears after project registry refresh.
- No code change made in this loop. If `/workspace/projects/src` writes continue in the next cycle, the smallest fix is to change `patch_code` default project directory from `/workspace/projects` to a slugged project directory and update build-trigger prompts to forbid direct writes under `/workspace/projects/src`.


## 2026-06-13 — observe loop follow-up: review gate and pending queue

- Runtime: containers remained up around two hours after deploy. Git still has only this observe log modified on top of `1f7f2d1 fix: gate review packets and ground workspace paths`.
- Heartbeat: manual `prod-heartbeat` returned `status: ok` in `45122ms`, improved from the prior loop's `111426ms`. It evaluated 30 triggers, fired 3, learned 1 memory, and reported 111 manifest entries today with synthesis success still at 88% and 2 blocked synthesis sessions.
- Review gate: active and now visibly creating a small backlog. DB status counts: `needs_revision=5`, `review=3`, `rejected=37`, `published=4`. All 5 recent `needs_revision` drafts were rejected for the same reason: missing opening metadata/reviewer context. This is likely too strict for synthesized artifacts unless artifact metadata is injected into the generated draft body or the preflight accepts DB metadata.
- Queue state: recent `ops_agent_sessions` created in the last 45 minutes were all `pending` (`16`). Worker logs show it is actively processing sessions, so this is a watch item rather than a failure yet. Next loop should confirm the queue drains instead of compounding.
- LLM/tool symptoms in the recent worker log sample: 6 `Ollama model returned empty text`, 7 `consecutiveEmptyRounds=2`, 1 `consecutiveEmptyRounds=3`, 27 `File read failed`, 4 `/workspace/src`, 7 `/workspace/projects/src`, 2 `/workspace/workspace`, 1 Brave fallback, and 1 failed `cast_veto` where the agent first used `droid-...` as a UUID before retrying successfully with the session UUID.
- Output quality: the newest milestone doc is valid but low-value/repetitive. The latest audit artifact remains weak and pre-gate: unsupported claims about missing `/output/reports/` and inaccessible inbox, no evidence table. Need to wait for a fresh post-gate audit session to verify whether new audit evidence enforcement blocks or improves this class.
- Workspace hygiene: permission scan clean. `workspace-hygiene.json` still absent, implying the project registry refresh path has not run since deploy.
- No code change made in this loop. If the next loop confirms the same patterns, the smallest fixes are: (1) relax/adjust review preflight to accept structured DB metadata for synthesized drafts or inject metadata into draft bodies, (2) change `patch_code` default from `/workspace/projects` to a slugged project directory and forbid `/workspace/projects/src`, and (3) add a `cast_veto` input guard that rejects non-UUID target IDs before DB insertion.


## 2026-06-13 — observe loop initial report: queue and evidence quality

- Runtime: Subcorp containers remained up around two hours after deploy. Git still has observe-log-only changes on top of `1f7f2d1 fix: gate review packets and ground workspace paths`.
- Heartbeat: manual `prod-heartbeat` returned `status: ok` in `20943ms`, faster than the prior manual checks. It evaluated 30 triggers, fired 0 in the manual response, enqueued one roundtable (`704f461f-bd0b-4d47-a937-7762a475db3e`), evaluated 12 cron schedules, fired 0, and reported 112 manifest entries today with synthesis success at 90% and 2 blocked synthesis sessions.
- Review gate: still active. DB status counts remain `needs_revision=5`, `review=3`, `rejected=37`, `published=4`; the same five recent `needs_revision` drafts are all blocked for missing opening metadata/reviewer context. This has not grown in this loop, so wait one more cycle before changing the gate.
- Queue state: DB shows 19 sessions created in the last 60 minutes still `pending` (`15` mission, `4` droid), and oldest pending sessions date back to ~17:17 UTC. Worker logs show active processing, but queue age is now the main watch item.
- LLM/tool symptoms in the recent worker log sample: 7 `Ollama model returned empty text`, 9 `consecutiveEmptyRounds=2`, 1 `consecutiveEmptyRounds=3`, 29 `File read failed`, 3 `/workspace/src`, 0 `/workspace/projects/src`, 0 `/workspace/workspace`, 7 Brave rate-limit fallbacks, 0 `audit evidence missing`, 0 `agent_session_blocked`, 4 `Failed to cast veto`, 1 `systemctl: command not found`, and 8 directory-read errors. Path guidance helped with `/workspace/projects/src`, but agents are still reading directories as files and trying unavailable host tools inside toolbox context.
- Output quality: the newest content review is concrete and useful; the newest revision note includes proper frontmatter and a usable public template. The newest security audit remains weak and violates the spirit of the new evidence gate: it lists findings about missing inbox/output reports and `systemctl` without a proper evidence table. Because `audit evidence missing` did not appear in logs, watch whether this was produced by a path that bypassed the new gate or merely before enforcement applied.
- Workspace hygiene: permission scan returned clean. `workspace-hygiene.json` is still absent, so the project registry refresh path likely has not run since deploy.
- No code change made in this loop. Next loop should decide based on evidence: if pending queue age grows, inspect worker concurrency/stuck claim behavior first; if `needs_revision` grows, adjust review preflight or inject metadata; if weak audits keep writing, tighten audit evidence detection; if `cast_veto` failures continue, add a non-UUID input guard.


## 2026-06-13 20:36 UTC — observe loop: backlog and review gate

- Runtime: `subcorp-app`, `subcorp-worker`, `subcorp-toolbox`, and `subcorp-sanctum` are up for ~3 hours. Git is still otherwise clean on `1f7f2d1 fix: gate review packets and ground workspace paths`; only this observe log is modified.
- Heartbeat: manual `prod-heartbeat` returned `status: ok` in `102336ms`. It evaluated 30 triggers, fired 4, learned 1 memory, evaluated 12 cron schedules, fired 0, and reported 114 manifest entries today with synthesis success at 90% and 2 blocked synthesis sessions. Heartbeat duration regressed from the prior loop's `20943ms`, likely because it fired more triggers.
- Queue state: backlog is now the main bottleneck. DB shows 53 total pending sessions, oldest pending at `17:47 UTC`, and 21 sessions created in the last 60 minutes still pending (`17` mission, `2` conversation, `2` droid). One session is currently running. Recent starts show the worker is still making progress, but throughput is behind intake.
- Review gate: `needs_revision` grew from 5 to 6, while `review=3`, `rejected=38`, and `published=4`. All six `needs_revision` drafts share the same reason: missing opening metadata/reviewer context. This confirms the previous watch item: the gate is useful but probably too strict for generated/synthesized drafts unless metadata is injected into draft bodies or accepted from DB metadata.
- LLM/tool symptoms in the last 60 minutes of worker logs: 7 `Ollama model returned empty text`, 5 `consecutiveEmptyRounds=2`, 2 `consecutiveEmptyRounds=3`, 17 `File read failed`, 0 `/workspace/src`, 0 `/workspace/projects/src`, 0 `/workspace/workspace`, 8 Brave-related fallback mentions, 0 `agent_session_blocked`, 3 `Failed to cast veto`, and 0 worker heartbeat failures. Path-drift appears improved; file-read misses and empty local-model rounds remain noisy.
- Output quality: newest product spec has correct frontmatter and coherent structure, but is generic and not tied to the actual Subcorp/platform constraints. The newest critique is readable but praises a weak pre-gate security audit as technically sound. The referenced security audit is still low quality and factually stale: it claims `/output/reports/` is empty despite many reports existing. Audit evidence enforcement did block at least one later audit session, so the next loop should wait for fresh post-gate audit artifacts before changing code.
- Workspace hygiene: world-writable scan returned clean. `workspace-hygiene.json` is still absent, so registry refresh/hygiene report generation still has not visibly run.
- No code/config change made in this cycle. Watch next: whether pending backlog drains below 53 and oldest pending advances past `17:47 UTC`; whether `needs_revision` grows beyond 6; whether fresh audits are blocked or include evidence tables; continued `cast_veto` failures; and whether heartbeat duration stays near 100s when triggers fire. If backlog grows next loop, inspect worker concurrency/claim cadence and trigger intake before changing content-review behavior.


## 2026-06-13 21:04 UTC — observe loop: audit path tune

- Runtime: `subcorp-app`, `subcorp-worker`, `subcorp-toolbox`, and `subcorp-sanctum` remain up for ~3 hours. Git has the prior observe log plus this cycle's prompt/test changes on top of `1f7f2d1 fix: gate review packets and ground workspace paths`.
- Heartbeat: manual `prod-heartbeat` returned `status: ok` in `24357ms`, much faster than the prior manual `102336ms` because it fired 0 triggers. It evaluated 30 triggers, learned 0 memories, evaluated 12 cron schedules, fired 0, and reported 119 manifest entries today with synthesis success still at 90% and 2 blocked synthesis sessions.
- Queue state: backlog improved but has not cleared. Total pending sessions dropped from 53 to 43, and oldest pending advanced from `17:47 UTC` to `18:43 UTC`. Last-60-minute pending count is 20 (`18` mission, `2` conversation). Two sessions are running, and recent worker logs show 28 processing starts in the last hour, so throughput exists but remains behind intake.
- Review gate: stable this cycle. Draft counts remain `needs_revision=6`, `review=3`, `rejected=38`, `published=4`. All `needs_revision` drafts still fail for the same missing opening metadata/reviewer context, but the count did not grow; keep watching before changing content-review behavior.
- LLM/tool symptoms in the last 60 minutes of worker logs improved: 5 `Ollama model returned empty text`, 3 `consecutiveEmptyRounds=2`, 1 `consecutiveEmptyRounds=3`, 8 `File read failed`, 0 `/workspace/src`, 0 `/workspace/projects/src`, 0 `/workspace/workspace`, 8 Brave-related fallback mentions, 0 `agent_session_blocked`, 0 `Failed to cast veto`, and 0 worker heartbeat failures.
- Output quality: newest milestone lesson is concrete but repeats stale/false findings about empty `/output/reports` and permissive inbox permissions. Newest audit includes an evidence table, which is progress, but its evidence uses bare `/output/reports` and `/agents/...` paths and falsely claims reports are absent despite `/workspace/output/reports` containing many artifacts. A droid audit has the same host/toolbox confusion (`ss`/`systemctl` unavailable). Goal alignment score: 6/10 — the loop is moving work, but audit quality remains the limiting factor.
- Tune applied: tightened audit path guidance in `src/lib/ops/step-prompts.ts` so audit prompts explicitly require real `/workspace/...` paths, name `/workspace/output` and `/workspace/agents`, forbid bare `/output`, `/agents`, and `/projects`, and require verifying the corresponding `/workspace` path before claiming missing artifacts. Updated `tests/step-prompts-regression.test.ts` for this contract.
- Live config applied: updated the `ops_step_templates` row for `audit_system` from version 1 to version 2 with the same path/evidence rule, so future audit sessions can pick it up without an app/worker rebuild.
- Validation: `bun test tests/step-prompts-regression.test.ts` passed and `npx tsc --noEmit` passed. Workspace world-writable scan returned clean; `workspace-hygiene.json` is still absent.
- Watch next loop: whether fresh audit artifacts after template version 2 use `/workspace/output` and stop claiming empty reports; whether pending backlog drains below 43 and oldest pending advances past `18:43 UTC`; whether `needs_revision` stays at 6 or grows; and whether file-read misses continue falling. If audit artifacts still use bare paths after fresh version-2 sessions, inspect DB template caching/deployment and consider a worker rebuild.


## 2026-06-13 21:38 UTC — observe loop: file_read directory tune

- Runtime: containers are healthy but recently restarted; `subcorp-app`, `subcorp-worker`, `subcorp-toolbox`, and `subcorp-sanctum` are all up for about 19 minutes. Git already has unrelated working tree changes outside this observe cycle (`.env.example`, `README.md`, toolbox sync script, situational briefing, Gitea regression test, and migration 032); this cycle intentionally changed only observe notes, step/file-read prompt guidance, and related regression tests.
- Heartbeat: manual `prod-heartbeat` returned `status: ok` in `70464ms`. It evaluated 30 triggers, fired 2, learned 3 memories, queued 4 initiatives, evaluated 12 cron schedules, fired 0, and reported 124 manifest entries today with synthesis success at 88% and 2 blocked synthesis sessions.
- Queue state: backlog continues to drain. Total pending sessions dropped from 43 to 29, and oldest pending advanced from `18:43 UTC` to `19:47 UTC`; last-60-minute pending count is 10 (`9` mission, `1` conversation). Recent completions include 10 succeeded, 23 blocked, and 1 timed out. Two droid sessions are currently running, both started around `21:32 UTC` after the container restart.
- Review gate: stable. Draft counts remain `needs_revision=6`, `review=3`, `rejected=38`, `published=4`. All `needs_revision` drafts still have the same missing-opening-metadata reviewer note, and the count did not grow this cycle.
- LLM/tool symptoms in the last 60 minutes of worker logs: 6 `Ollama model returned empty text`, 2 `consecutiveEmptyRounds=2`, 0 `consecutiveEmptyRounds=3`, 17 `File read failed`, 1 `/workspace/projects/src`, 0 `/workspace/src`, 0 `/workspace/workspace`, 0 Brave mentions, 0 `Failed to cast veto`, 0 `agent_session_blocked`, and 16 processing starts. The remaining file-read misses are mostly directory reads (`/workspace/output/`, `/workspace/agents/.../notes/`) or assumed files like `roadmap.md`, `approval_status.txt`, and `repo_list.txt`.
- Output quality: latest artifacts are still uneven. A droid audit for publication redaction GA is low-quality and unsupported, citing missing `repo_list.txt` and `approval_status` without an evidence table. A milestone lesson is short but low-value. A directory-creation droid result is concrete and correct. A receipt-contact-flow product spec has frontmatter and structure but is generic and over-specified, with biometric/distributed-ledger requirements not grounded in observed Subcorp constraints. Goal alignment score: 6/10 — throughput and queue health improved, but output factuality remains limited by tool/path misuse and generic drafting.
- Tune applied: tightened `file_read` guidance. `src/lib/ops/step-prompts.ts` now prepends a file-read rule to DB-rendered prompts that mention `file_read`, telling agents that `file_read` accepts concrete files only and to use bash to list directories first; the hardcoded `patch_code` prompt also includes this rule. `src/lib/tools/tools/file-read.ts` now advertises concrete-file-only usage, names `/workspace/output` and `/workspace/agents`, and returns a directory-specific hint when a path ends with `/`. Updated `tests/step-prompts-regression.test.ts` and `tests/workspace-hygiene-regression.test.ts` for the contract.
- Live config applied: prefixed the same file-read rule into live `ops_step_templates` rows that mention `file_read` and were not already guarded: `draft_essay` v2, `draft_thread` v2, `critique_content` v2, `distill_insight` v2, `consolidate_memory` v2, and `patch_code` v51. This should affect future sessions without waiting for a rebuild.
- Validation: `rtk bun test tests/step-prompts-regression.test.ts tests/workspace-hygiene-regression.test.ts` passed (4 tests, 24 expects). `rtk npx tsc --noEmit` passed. Workspace world-writable scan returned clean; `/workspace/shared/workspace-hygiene.json` is still absent.
- Watch next loop: whether file-read failures fall below 17 and stop targeting directories; whether fresh audits include evidence tables and avoid bare `/output`/assumed inventory files; whether pending drains below 29 and oldest advances past `19:47 UTC`; whether `needs_revision` remains stable; and whether generic product specs become more grounded in concrete workspace/context artifacts.


## 2026-06-13 22:39 UTC — observe loop: audit path enforcement

- Runtime: `subcorp-app`, `subcorp-sanctum`, `subcorp-toolbox`, and `subcorp-worker` were healthy. The worker was rebuilt/restarted after this cycle's small code tune and came back up cleanly. Git still includes pre-existing unrelated changes (`.env.example`, `README.md`, toolbox sync script, situational briefing, Gitea regression test, and migration 032); this cycle intentionally changed only this observe log, `src/lib/tools/agent-session.ts`, and `tests/review-preflight-regression.test.ts`.
- Heartbeat: pre-tune manual `prod-heartbeat` returned `status: ok` in `66150ms`, with 30 triggers evaluated, 0 fired, 1 memory learned, 135 manifest entries today, synthesis success at 95%, and 2 blocked synthesis sessions. Post-restart heartbeat returned `status: ok` in `24677ms`, with 136 manifest entries today and no underperforming templates reported.
- Queue state: backlog cleared sharply. Total pending dropped from 29 to 2, with oldest pending now `22:26 UTC`; one audit session was running. Recent completions in the last hour were 17 succeeded and 20 blocked, showing high throughput but still a meaningful blocked fraction.
- Review gate: `needs_revision` grew from 6 to 7 while `review=3`, `rejected=38`, and `published=4`. All needs-revision drafts still share the same missing opening metadata/reviewer-context note. This remains stable enough to watch, but it is now the next likely quality bottleneck if it keeps growing.
- LLM/tool symptoms in the last 60 minutes of worker logs: 13 `Ollama model returned empty text`, 9 `consecutiveEmptyRounds=2`, 0 `consecutiveEmptyRounds=3`, 22 `File read failed`, 0 `/workspace/src`, 0 `/workspace/projects/src`, 1 `/workspace/workspace`, 12 Brave mentions, 0 `Failed to cast veto`, 0 `agent_session_blocked`, and 36 processing starts. File-read failures did not fall yet, but the sample still spans sessions created before the file-read guidance/restart; continue watching post-restart sessions specifically.
- Output quality: the newest audit artifact was generated after the audit template v2 prompt was present in the session, but it still violated the path contract by citing bare `/output/reviews` and `/output/reports`, falsely claiming reports were absent, and making a Gitea-process claim from container-local evidence. The prompt contained the audit path rule, so this is no longer just a prompt/template propagation issue. Other recent artifacts were mixed: a versioned exit receipt template was concrete but generic, an essay rewrite example was too thin, and a product spec remained overconfident/generic. Goal alignment score: 6/10 — runtime/queue health improved, but audit factuality still needs enforcement rather than more prompting.
- Tune applied: tightened the audit success gate in `src/lib/tools/agent-session.ts`. `detectAuditEvidenceIssues` now rejects audit `file_write` evidence that contains bare `/output`, `/agents`, or `/projects` paths and adds explicit blocked evidence: `audit path evidence invalid` plus `audit_system outputs must use real /workspace/... paths for commands and evidence`. This turns the recurring bare-path audit failure into a blocked session instead of allowing a misleading success artifact.
- Validation/deploy: `rtk bun test tests/review-preflight-regression.test.ts tests/step-prompts-regression.test.ts tests/workspace-hygiene-regression.test.ts` passed (6 tests, 35 expects). `rtk npx tsc --noEmit` passed. Rebuilt and restarted `subcorp-worker` with `docker compose ... up -d --build subcorp-worker`; post-restart `docker compose ps` and `prod-heartbeat` were healthy. Workspace world-writable scan returned clean; `/workspace/shared/workspace-hygiene.json` is still absent.
- Watch next loop: whether fresh post-rebuild audits with bare `/output` now block instead of succeeding; whether audit artifacts use `/workspace/output` evidence; whether file-read failures decline in logs after the restart window; whether pending stays near zero; and whether `needs_revision` grows beyond 7, which would justify a small metadata-injection/preflight tune.


## 2026-06-13 23:02 UTC — observe loop: post-enforcement watch

- Runtime: `subcorp-app`, `subcorp-sanctum`, `subcorp-toolbox`, and `subcorp-worker` are healthy. Worker has been up ~21 minutes since the prior audit-enforcement rebuild; app/sanctum have been up ~2 hours. Git still includes pre-existing unrelated changes plus the observe-cycle files; no new code/config change was made in this loop.
- Heartbeat: manual `prod-heartbeat` returned `status: ok` in `23889ms`. It evaluated 30 triggers, fired 2, enqueued one roundtable (`6de8e8f6-4be3-46d8-a449-6e740dd78f0c`), evaluated 12 cron schedules, fired 0, and reported 141 manifest entries today with synthesis success at 95% and 2 blocked synthesis sessions. Runtime health is good.
- Queue state: pending remains low compared with earlier loops, but has grown from 2 to 6 after the heartbeat fired new work. Oldest pending is now `22:49 UTC`; two sessions are running. Recent completions in the last hour were 13 succeeded and 15 blocked. Throughput is acceptable; the blocked fraction remains the main watch item.
- Review gate: stable at `needs_revision=7`, `review=3`, `rejected=38`, `published=4`. All needs-revision drafts still share the same missing opening metadata/reviewer-context note. Do not change the review gate yet unless the count grows again or blocks publication flow.
- LLM/tool symptoms in the post-restart worker log window are much cleaner: 0 `Ollama model returned empty text`, 0 `consecutiveEmptyRounds=2`, 0 `consecutiveEmptyRounds=3`, 0 `File read failed`, 0 `/workspace/src`, 0 `/workspace/projects/src`, 0 `/workspace/workspace`, 2 Brave mentions, 0 `Failed to cast veto`, and 9 processing starts. This is an early positive signal that the file-read guidance plus worker restart reduced noise; keep watching because the window is still short.
- Audit enforcement: the fresh post-enforcement pending-proposals audit with bare `/output`/`/agents` evidence was correctly marked `blocked` with `audit path evidence invalid`. This confirms the prior tune is active. A newer audit artifact for machine-readable task status codes uses `/workspace/...` paths, but its quality is still uneven: it claims `agent-session.ts` was inaccessible and recommends adding status columns despite `ops_agent_sessions.status` already existing. That session is still running in DB, so wait for completion/blocking before applying another audit tune.
- Output quality: recent docs/lesson artifacts remain generic and overclaim outcomes (for example, invented coverage/compliance improvements for task status codes). The newest audit has a proper evidence table shape and avoids bare `/output`, but the evidence is still not reliably grounded. Goal alignment score: 6/10 — operational health is better and enforcement is working, but substantive artifact quality remains uneven.
- Tune decision: no new code/config change this cycle. The smallest high-leverage fix from the last cycle is now verified, and remaining issues need another loop of post-rebuild evidence before changing prompts or gates again.
- Workspace hygiene: world-writable scan returned clean. `/workspace/shared/workspace-hygiene.json` is still absent.
- Watch next loop: whether the running machine-readable-status audit succeeds or blocks; whether post-restart file-read failures stay near zero; whether blocked sessions remain high due to empty no-tool rounds; whether pending stays under ~10 and oldest pending advances; whether `needs_revision` grows beyond 7; and whether generic lesson/product artifacts continue to invent metrics or implementation outcomes.


## 2026-06-13 23:40 UTC — observe loop: draft metadata backfill

- Runtime: `subcorp-app`, `subcorp-sanctum`, and `subcorp-toolbox` remained healthy; `subcorp-worker` was rebuilt/restarted after this cycle's content-draft metadata tune and came back up cleanly. Git still includes pre-existing unrelated changes (`.env.example`, `README.md`, toolbox sync script, situational briefing, Gitea regression test, and migration 032); this cycle intentionally changed this observe log, `src/lib/ops/content-pipeline.ts`, `scripts/unified-worker/index.ts`, and `tests/review-preflight-regression.test.ts`.
- Heartbeat: pre-tune manual `prod-heartbeat` returned `status: ok` in `30191ms`, evaluated 30 triggers, fired 1, queued 4 initiatives, and reported 146 manifest entries today with synthesis success at 95% and 2 blocked synthesis sessions. Post-tune heartbeat returned `status: ok` in `27101ms` with no trigger fires and no underperforming templates in the visible output.
- Queue state: pending remains controlled but is no longer near-zero. DB showed 9 pending sessions and 1 running session, with the oldest active item created at `23:02 UTC`; recent last-hour completions were 12 succeeded and 8 blocked. Throughput is acceptable, but blocked sessions remain a watch item.
- Review gate: `needs_revision` grew from 7 to 8 with the same repeated reason: missing opening metadata/reviewer context. This is now the clearest recurring bottleneck. Live tune backfilled the eight affected drafts with a review-ready metadata envelope (`artifact_id`, `source_session`, `version`, `audience`, `publish_target`, `content_type`, `title`, `reviewer_ask`), reset them to `draft`, and cleared reviewer notes so the normal review trigger can re-evaluate them. After backfill, draft counts were `draft=8`, `review=3`, `rejected=38`, `published=4`.
- LLM/tool symptoms in the last 60 minutes of worker logs stayed improved versus pre-restart cycles: 1 `Ollama model returned empty text`, 1 `consecutiveEmptyRounds=2`, 0 `consecutiveEmptyRounds=3`, 2 `File read failed`, 0 `/workspace/src`, 0 `/workspace/projects/src`, 0 `/workspace/workspace`, 1 Brave mention, 0 `Failed to cast veto`, and 21 processing starts. Remaining file-read misses were specific missing files rather than directory misuse.
- Output quality: newest content artifacts are still mixed. The data-privacy risk review is coherent and directionally useful. The task-status lesson and patch note overclaim implementation details that are not reflected in the codebase, so artifact factuality remains uneven. The latest pending-proposals audit used real `/workspace/...` paths and evidence table shape, which is an improvement over bare-path audits.
- Tune applied: source-level draft creation now injects the same review packet metadata before drafts hit the preflight gate. `src/lib/ops/content-pipeline.ts` covers the older extraction path; `scripts/unified-worker/index.ts` covers the live synthesis-to-draft path. The worker was rebuilt so future synthesis drafts should include the metadata envelope immediately.
- Validation/deploy: `rtk bun test tests/review-preflight-regression.test.ts` passed, `rtk npx tsc --noEmit` passed, `docker compose ... up -d --build subcorp-worker` succeeded, post-rebuild `docker compose ps` and `prod-heartbeat` were healthy, and the workspace world-writable scan returned clean. `/workspace/shared/workspace-hygiene.json` is still absent.
- Watch next loop: whether the 8 backfilled drafts move from `draft` to `review` instead of returning to `needs_revision`; whether new synthesis drafts include the metadata envelope without DB backfill; whether pending stays under ~10; whether file-read failures stay near zero; whether blocked sessions are mostly legitimate evidence gates; and whether lesson/patch artifacts stop inventing implementation outcomes.


## 2026-06-14 00:08 UTC — observe loop: unsupported audit evidence gate

- Runtime: `subcorp-app`, `subcorp-sanctum`, and `subcorp-toolbox` remained healthy; `subcorp-worker` was rebuilt/restarted after this cycle's audit evidence tune and came back up cleanly. Git still includes pre-existing unrelated changes (`.env.example`, `README.md`, toolbox sync script, situational briefing, Gitea regression test, and migration 032); this cycle intentionally changed this observe log, `src/lib/tools/agent-session.ts`, and `tests/review-preflight-regression.test.ts`.
- Heartbeat: pre-tune manual `prod-heartbeat` returned `status: ok` in `50613ms`, evaluated 30 triggers, fired 2, enqueued one roundtable (`5df818aa-1c7b-4f5f-8144-b6d36072d0dc`), queued 0 initiatives, fired 1 cron schedule, and showed the new UTC-day artifact counter at 1 manifest entry. Post-rebuild heartbeat returned `status: ok` in `22803ms` with no trigger fires and clean workspace permissions.
- Queue state: pending remains controlled. DB showed 6 pending and 3 running sessions, with oldest active work created at `23:09 UTC`; last-75-minute completions were 17 succeeded and 8 blocked. Recent blocked sessions were mostly evidence gates (`audit evidence missing`, `Required tool evidence missing`) plus one fatal tool error, so the worker is processing but still rejecting low-evidence work.
- Review gate: the previous metadata tune is working better than the raw count suggests. `needs_revision` is now 0; counts are `draft=7`, `review=3`, `rejected=39`, `published=4`. Of the 8 backfilled drafts, one moved through review and was rejected for substantive reasons, while 7 remain draft with metadata envelopes and empty reviewer notes. No `Content draft failed review preflight` appeared in worker logs. Continue watching whether the remaining 7 draft items move into review without reentering `needs_revision`.
- LLM/tool symptoms in the last 60 minutes of worker logs remain much cleaner than earlier cycles: 0 `Ollama model returned empty text`, 0 `consecutiveEmptyRounds=2`, 0 `consecutiveEmptyRounds=3`, 3 `File read failed`, 0 `/workspace/src`, 0 `/workspace/projects/src`, 0 `/workspace/workspace`, 0 Brave mentions, 0 `Failed to cast veto`, and 8 processing starts. Remaining file-read misses were concrete missing-file guesses (`task_manager.py` and generated artifact paths), not directory reads.
- Output quality: recent artifacts remain mixed. The data privacy review is coherent and useful. The newest milestone lesson is generic but harmless. Machine-readable-status outputs continue to overclaim implementation details and generated paths. The latest pending-proposals audit improved by using `/workspace/...` paths and evidence table shape, but it still included a `N/A` command/source row to support a broad "No immediate security risks detected" claim, which violates the audit evidence contract.
- Tune applied: tightened the audit success gate in `src/lib/tools/agent-session.ts`. `detectAuditEvidenceIssues` now rejects audit `file_write` evidence that uses `N/A`, `none`, or `not applicable` as command/source evidence, and adds explicit blocked evidence: `audit unsupported evidence invalid` plus `audit_system outputs must support no-issue/no-risk claims with bash output or hostAudit evidence`. This complements the prior bare-path audit gate.
- Validation/deploy: `rtk bun test tests/review-preflight-regression.test.ts tests/step-prompts-regression.test.ts tests/workspace-hygiene-regression.test.ts` passed (6 tests, 46 expects). `rtk npx tsc --noEmit` passed. `docker compose ... up -d --build subcorp-worker` succeeded; post-rebuild `docker compose ps` and `prod-heartbeat` were healthy. Workspace world-writable scan returned clean; `/workspace/shared/workspace-hygiene.json` is still absent.
- Watch next loop: whether new audits with unsupported `N/A`/none evidence now block; whether audits can still succeed when every claim has bash/hostAudit backing; whether the 7 backfilled drafts advance to `review`; whether pending stays under ~10; whether file-read failures stay near zero; and whether generic lesson/patch artifacts stop inventing implementation outcomes.


## 2026-06-14 00:26 UTC — quality iteration: artifact grounding contract

- Goal: move toward 10/10 by addressing the highest remaining quality gap: lesson/spec/digest/patch artifacts stating implementation outcomes, metrics, schema changes, or completed work without evidence.
- Source tune: added an `ARTIFACT_GROUNDING_GUIDANCE` contract in `src/lib/ops/step-prompts.ts`. It tells agents not to claim code changes, schema changes, metrics, coverage, compliance, operational outcomes, or completed work unless verified with `bash`, `file_read`, `web_fetch`, or a cited source artifact; requires a `Grounding` section listing exact files/commands/DB rows/URLs/source artifacts; and requires unverified claims to be labelled as proposals, assumptions, or next steps.
- Prompt coverage: the grounding contract now applies to DB-rendered prompts for `document_lesson`, `draft_product_spec`, `distill_insight`, `draft_essay`, `draft_thread`, `critique_content`, and `patch_code`; hardcoded fallback prompts for `patch_code`, `distill_insight`, `document_lesson`, and `draft_product_spec` also include it.
- Live config applied: prefixed the same grounding rule into live `ops_step_templates` rows not already guarded: `draft_essay` v3, `document_lesson` v2, `draft_thread` v3, `critique_content` v3, `distill_insight` v3, and `patch_code` v58. `draft_product_spec` currently has no live DB row, so source fallback covers it.
- Validation/deploy: `rtk bun test tests/step-prompts-regression.test.ts tests/review-preflight-regression.test.ts tests/workspace-hygiene-regression.test.ts` passed (7 tests, 54 expects). `rtk npx tsc --noEmit` passed. Rebuilt/restarted `subcorp-worker` with `docker compose ... up -d --build subcorp-worker`; post-rebuild `docker compose ps` and `prod-heartbeat` were healthy. Workspace world-writable scan returned clean; `/workspace/shared/workspace-hygiene.json` is still absent.
- Watch next loop: whether new lesson/spec/digest/patch artifacts include `Grounding` sections; whether they stop inventing implementation outcomes and metrics; whether pending remains controlled; whether the backfilled drafts continue moving through review; and whether audit gates block unsupported `N/A` evidence while allowing fully evidenced audits to succeed.


## 2026-06-14 00:36 UTC — observe loop: patch template drift guard

- Runtime: `subcorp-app`, `subcorp-sanctum`, `subcorp-toolbox`, and `subcorp-worker` were healthy; worker had been up ~6–11 minutes after the prior grounding deploy. `prod-heartbeat` returned `status: ok` before and after the tune (22s then 19s), with no underperforming templates in visible output.
- Queue/review state: sessions were still controlled but rising after new work: `pending=14`, `running=3`, `succeeded=301`, `blocked=1248`, `timed_out=14`. Draft review stayed improved: `needs_revision=0`; backfilled drafts were progressing (`5 draft`, `3 rejected`). Recent blocks were mostly required-tool/audit-evidence gates rather than path drift.
- Logs: post-restart worker sample was clean: `File read failed=0`, empty-round warnings `0`, path drift `0`, audit evidence/path/unsupported invalid log noise `0`, failed veto `0`, workspace permissions clean (`world_writable_files=0`). `/workspace/shared/workspace-hygiene.json` remains absent.
- Output quality: newest `log_event` notes still overclaimed meeting facts without grounding, and recent `patch_code` changelogs falsely described code changes without a `Grounding` section. The live `patch_code` DB template had drifted to version 60 and no longer contained workspace path, file_read, or artifact grounding rules, likely from the pending workspace/Gitea owner split migration/template update.
- Tune: patched `db/migrations/032_workspace_gitea_owner_split.sql` so future `patch_code` template updates preserve workspace path guidance, file_read concrete-file guidance, and artifact grounding/changelog requirements. Re-applied the same guidance to the live `ops_step_templates.patch_code` row (`version=61`) so new patch missions pick it up without rebuild.
- Validation: `rtk bun test tests/agent-tooling-gitea-regression.test.ts tests/step-prompts-regression.test.ts tests/review-preflight-regression.test.ts tests/workspace-hygiene-regression.test.ts` passed (18 tests, 123 expects). `rtk npx tsc --noEmit` passed. Post-tune Docker health and heartbeat passed.
- Next watch: confirm new `patch_code` artifacts include a `Grounding` section and stop claiming unverified code changes; consider extending grounding to `log_event` if it continues inventing meeting/security facts; keep `needs_revision=0`; pending should drain back under ~10; file-read/path/empty-round noise should stay near zero.


## 2026-06-14 01:10 UTC — observe loop: grounding enforcement gate

- Runtime: services were healthy before the tune; worker had been up ~35 minutes. `prod-heartbeat` returned `status: ok` in ~33s with 30 triggers evaluated/0 fired, 7 manifest entries today, synthesis success 75%, and no underperforming templates in visible output.
- Queue/review state: backlog rose with new work (`pending=21`, `running=1`, `succeeded=309`, `blocked=1255`, `timed_out=15`). Draft preflight remained fixed: `needs_revision=0`; backfilled drafts continued moving (`4 draft`, `4 rejected`). Recent blocks were mainly required-tool evidence/audit evidence/fatal tool error gates, not metadata preflight.
- Logs/workspace: sampled worker logs showed low path/read noise overall, though recent snippets included concrete missing-file reads and one empty qwen round during signal scanning. Workspace permissions remained clean (`world_writable_files=0`); `/workspace/shared/workspace-hygiene.json` remains absent.
- Output quality: the prior patch template drift was fixed in live config (`patch_code` v61 with workspace/file_read/grounding rules), but new `document_lesson`, `log_event`, `content_review`, and `self_evolution` artifacts still lacked `Grounding` sections and continued to state outcomes/meeting facts/code changes as facts. The prompt-only grounding rule was being ignored by some agents.
- Tune: added an execution gate in `src/lib/tools/agent-session.ts` for grounded artifact step kinds. `document_lesson`, `draft_product_spec`, `distill_insight`, `draft_essay`, `draft_thread`, `critique_content`, `patch_code`, `content_revision`, `self_evolution`, and `log_event` now block with `artifact grounding missing` if their artifact/summary writes lack a `Grounding` section. Expanded prompt grounding coverage to `content_revision`, `self_evolution`, and `log_event`; generic fallback prompts now prepend grounding guidance when the step kind needs it. Updated live `ops_step_templates.log_event` to v2 with the grounding rule.
- Validation/deploy: `rtk bun test tests/agent-tooling-gitea-regression.test.ts tests/step-prompts-regression.test.ts tests/review-preflight-regression.test.ts tests/workspace-hygiene-regression.test.ts` passed (20 tests, 132 expects). `rtk npx tsc --noEmit` passed. Rebuilt/restarted `subcorp-worker`; post-rebuild Docker health and `prod-heartbeat` passed (~22s), and workspace permissions stayed clean.
- Next watch: newly completed grounded steps should either include a real `Grounding` section or block with `artifact grounding missing`; confirm this does not over-block source-only writes that also produce grounded changelogs. Pending should drain from 21; `needs_revision` should stay 0; file-read/path/empty-round noise should remain low.


## 2026-06-14 01:34 UTC — observe loop: residual draft metadata cleanup

- Runtime: services remained healthy (`subcorp-app`, `subcorp-sanctum`, `subcorp-toolbox`, and `subcorp-worker` up; worker ~21 minutes since the grounding-gate rebuild). `prod-heartbeat` returned `status: ok` in ~21s with 30 triggers evaluated/0 fired, 4 initiatives queued, 8 manifest entries today, synthesis success 60%, and no underperforming templates in visible output.
- Queue/review state: backlog stayed moderate but not cleared: `pending=17`, `running=2`, `succeeded=311`, `blocked=1264`, `timed_out=15`. Recent 75m activity was `blocked=11`, `pending=17`, `running=2`, `succeeded=4`; oldest pending was 00:54 UTC. Draft review mostly stayed fixed but one residual draft re-entered `needs_revision` with the old missing-metadata preflight reason.
- Logs/workspace: recent worker sample was much cleaner than earlier cycles: `File read failed=3` (concrete missing-note paths, not directory reads), empty-round warning counts `0`, path drift `0`, audit path/unsupported invalid log noise `0`, failed veto `0`, and no new grounding-block log lines yet. Workspace permissions remained clean (`world_writable_files=0`); `/workspace/shared/workspace-hygiene.json` remains absent.
- Output quality: newest artifacts were mostly from before the grounding execution gate took effect and still lacked `Grounding` sections (content review, self-evolution note/PR, milestone lesson). A newer audit used an evidence table and real `/workspace/...` paths, but its claims still need continued review for stale permission assumptions. No post-gate grounded artifact completion was available yet, so no additional prompt/code gate change was justified.
- Tune: backfilled the single residual `needs_revision` draft (`Product Brainstorm: Framebreak – The Email-First Decision Engine for Solo Founders`) with the review-ready metadata envelope, reset it to `draft`, cleared reviewer notes, and tagged metadata with `review_packet_metadata_backfilled`. Draft counts after cleanup: `draft=4`, `review=2`, `rejected=43`, `published=4`, `needs_revision=0`.
- Validation: no rebuild needed; this was a live DB cleanup only. Verified draft counts after the update. Docker health, heartbeat, and workspace permission checks were already healthy in this cycle.
- Next watch: confirm `needs_revision` stays at 0 after the residual backfill; wait for post-gate grounded steps to complete and verify they either include `Grounding` or block with `artifact grounding missing`; watch pending drain below ~10; keep file-read/path/empty-round noise low.


## 2026-06-14 02:02 UTC — observe loop: grounding gate first effects

- Runtime: all Docker services remained healthy; `subcorp-worker` had been up ~50 minutes since the grounding-gate rebuild. `prod-heartbeat` returned `status: ok` in ~25s with 30 triggers evaluated/5 fired, one roundtable enqueued, one initiative queued, 11 manifest entries today, synthesis success 67%, and no underperforming templates in visible output.
- Queue/review state: backlog increased instead of draining: `pending=21`, `running=2`, `succeeded=311`, `blocked=1273`, `timed_out=16`. Oldest pending advanced to 01:19 UTC, but recent throughput was weak (`recent 90m: blocked=18`, `succeeded=2`, `pending=21`). Draft preflight stayed fixed after the prior residual cleanup: `needs_revision=0`, with `draft=3`, `review=3`, `rejected=43`, `published=4`.
- Gate outcomes: the new grounding gate is active and produced the first expected block: `document_lesson` session `7c617bcb-...` blocked with `artifact grounding missing`. Later grounded artifacts started including `Grounding` sections, so the gate is changing behavior rather than silently failing.
- Logs/workspace: worker logs were mostly stable but noisier than the prior cycle: `File read failed=10` (concrete missing paths, no directory misuse), `empty text=5`, `consecutiveEmptyRounds=2=3`, `consecutiveEmptyRounds=3=1`, path drift `0`, audit invalid log noise `0`, failed veto `0`, and one Brave fallback. Workspace permissions remained clean (`world_writable_files=0`); `/workspace/shared/workspace-hygiene.json` remains absent.
- Output quality: newest post-gate artifacts improved structurally. The product spec, patch note, and lesson all included `Grounding` sections. Quality is still not 10/10: the product spec cited likely-missing source paths and stated success metrics as targets without clearly labelling them as proposed; the patch note claimed an `intake_gate.py` endpoint from its own file-write evidence; the lesson grounding was thin but present. Older content review/audit artifacts without `Grounding` were pre-gate or separately gated.
- Tune decision: no code/config change this cycle. The last tune needs more post-gate samples: it has already blocked one ungrounded lesson and produced newer artifacts with `Grounding`. The next bottleneck appears to be grounding quality (verified vs proposed/source existence), but another gate should wait until more post-gate artifacts complete to avoid overfitting.
- Next watch: pending should drain from 21 and success rate should recover; post-gate artifacts should keep `Grounding` sections and improve from “section present” to verified evidence; watch for nonexistent paths in `Grounding`; keep `needs_revision=0`; monitor whether concrete missing-file reads and empty qwen rounds rise again.


## 2026-06-14 02:32 UTC — observe loop: grounding gate holding, backlog rising

- Runtime: all Docker services remained healthy; worker had been up about an hour since the grounding-gate rebuild. `prod-heartbeat` returned `status: ok` in ~25s with 30 triggers evaluated/1 fired, 14 manifest entries today, synthesis success 50%, synthesis blocked 1, and no underperforming templates in visible output.
- Queue/review state: backlog worsened: `pending=32`, `running=1`, `succeeded=312`, `blocked=1285`, `timed_out=16`. Recent 90m throughput was poor (`blocked=11`, `succeeded=1`, `pending=32`), with oldest pending at 01:38 UTC. Draft preflight remains fixed: `needs_revision=0`, with `draft=1`, `review=3`, `rejected=45`, `published=4`.
- Gate outcomes: grounding enforcement is active and has now blocked two ungrounded `document_lesson` sessions with `artifact grounding missing`. There were no recent successful grounded-step sessions in DB yet, so post-gate success quality is still under-sampled. Audit gates are still blocking no-evidence audits as intended.
- Logs/workspace: worker logs were noisier than the previous loop: `File read failed=14` (concrete missing paths, still no directory misuse), `empty text=10`, `consecutiveEmptyRounds=2=7`, `consecutiveEmptyRounds=3=1`, Brave fallback 4, path drift `0`, failed veto `0`. Workspace permissions remain clean (`world_writable_files=0`); `/workspace/shared/workspace-hygiene.json` remains absent.
- Output quality: newer artifacts show mixed impact. Some artifacts include `Grounding`, but several are still products of blocked sessions or write-before-block behavior. The latest product specs include `Grounding`, but cite likely-missing paths and present target metrics as factual goals; the newest brainstorm report still lacks `Grounding` because that step kind is not currently gated. Content-review artifacts still lack grounding and may be blocked/rejected downstream, not publish-ready.
- Tune decision: no code/config change this cycle. The new grounding gate is catching ungrounded writes, but the main visible problem is now throughput/backlog plus low-quality blocked artifacts remaining on disk. A new gate for grounding-source existence or artifact quarantine would be higher risk without more completed post-gate samples. Let one more loop show whether pending drains and whether grounded-success artifacts appear.
- Next watch: pending must drain from 32 and success rate should recover; if backlog keeps rising, inspect worker concurrency/intake trigger volume before adding more quality gates. Track whether blocked grounded artifacts continue polluting `/workspace/output`; consider quarantine/delete-on-block only if repeated. Keep `needs_revision=0`; watch concrete missing-file reads and empty qwen rounds.


## 2026-06-14 03:54 UTC — observe loop: backlog concurrency tune

- Runtime: all Docker services stayed healthy. Worker had been up ~2 hours before the tune. `prod-heartbeat` returned `status: ok`, but the pre-tune run was slow (~169s) with 30 triggers evaluated/3 fired, one roundtable enqueued, 18 manifest entries today, synthesis success 50%, and 1 blocked synthesis session. Post-tune heartbeat returned `status: ok` in ~22s.
- Queue/review state: backlog worsened for a third consecutive loop. Pre-tune DB showed `pending=36`, `blocked=1298`, `succeeded=314`, `timed_out=16`, with no running sessions and recent 90m throughput only `succeeded=3` vs `blocked=12` and `pending=36`. After the worker restart, active backlog was still high (`pending=45`, `running=1`), so the next loop must measure drain rate after the new worker setting has time to operate.
- Draft/review health: content preflight stayed fixed: `needs_revision=0`, with `draft=1`, `review=3`, `rejected=46`, `published=4`.
- Grounding/audit gates: grounding gate remains active; two recent `document_lesson` sessions blocked with `artifact grounding missing`. No recent successful grounded-step DB rows were visible in the pre-tune query, though workspace artifacts show some grounded outputs. Audit gates continue blocking no-evidence audits.
- Logs/workspace: last-hour logs were noisy again: `empty text=8`, `consecutiveEmptyRounds=2=5`, `consecutiveEmptyRounds=3=1`, `File read failed=7`, one `/workspace/workspace` path drift, Brave fallback 7, no failed veto. Workspace permissions remained clean; `/workspace/shared/workspace-hygiene.json` remains absent.
- Output quality: latest grounded artifacts are structurally better but uneven. `distill_insight` and operational-status lesson include `Grounding`. Product specs include `Grounding` but still cite likely nonexistent paths or present target metrics as if already validated. Some report types not yet gated (`brainstorm`/`debate`) still lack `Grounding` and overstate consensus/implementation shape.
- Tune applied: raised `WORKER_AGENT_SESSION_CONCURRENCY` in `docker-compose.yml` from `2` to `3` to address sustained queue growth. Recreated only `subcorp-worker` with `docker compose ... up -d --no-build --force-recreate subcorp-worker`. Verified live worker env reports `WORKER_AGENT_SESSION_CONCURRENCY=3`.
- Validation: `docker compose config --quiet` passed, `rtk npx tsc --noEmit` passed, post-recreate `docker compose ps` and `prod-heartbeat` were healthy.
- Next watch: measure whether pending drains from ~45 and running sessions rise toward 3 without increasing empty/model failures; if backlog continues rising, inspect trigger intake volume and worker claim loop rather than adding more quality gates. Keep `needs_revision=0`; monitor grounding quality and blocked-artifact pollution.


## 2026-06-14 04:20 UTC — observe loop: concurrency tune settling

- Runtime: all Docker services remained healthy. Worker had been up ~23 minutes with `WORKER_AGENT_SESSION_CONCURRENCY=3`, and the live env check confirmed `3`. `prod-heartbeat` returned `status: ok` but remained slow pre-observation (~163s) with 30 triggers evaluated/1 fired, 23 manifest entries today, synthesis success 56%, and no underperforming templates in visible output.
- Queue/review state: the concurrency tune is active but not yet proven sufficient. Running sessions rose to 3 as intended, but pending remained high: `pending=44`, `running=3`, `blocked=1323`, `succeeded=319`, `timed_out=16`. Oldest pending advanced from 02:32 to 02:55 UTC, so some drain occurred, but recent 90m throughput was still weak (`succeeded=1`, `blocked=2`, `pending=44`). Draft preflight stayed fixed: `needs_revision=0`, with `review=2`, `rejected=49`, `published=4`.
- Gate outcomes: grounding enforcement continued working. A newer `draft_product_spec` session blocked with `artifact grounding missing` despite its final summary mentioning a Grounding section; the block evidence said artifact writes lacked the required section, confirming the gate is checking actual writes, not summaries. Audit evidence gates also continued blocking no-evidence audit sessions.
- Logs/workspace: last-hour logs had `empty text=8`, `consecutiveEmptyRounds=2=5`, `consecutiveEmptyRounds=3=1`, `File read failed=7`, one `/workspace/workspace` path drift, Brave fallback 7, no failed veto, and no directory-read misuse. Workspace permissions remained clean; `/workspace/shared/workspace-hygiene.json` remains absent.
- Output quality: latest artifacts show mixed but somewhat improved structure. `distill_insight` and operational-status lesson include `Grounding`; some product specs still cite weak or likely missing sources and treat targets as facts. `brainstorm`/`debate` reports still lack Grounding because those step kinds are not currently gated. Several blocked sessions still leave visible output artifacts, so output directories contain non-publishable work unless consumers check DB status.
- Tune decision: no new code/config change this cycle. The previous concurrency tune has only had ~20 minutes to operate and did increase running sessions to 3. Raising concurrency again or adding more quality gates now would confound the signal and may worsen empty/model noise. Let one more loop measure whether pending drains below ~44 and whether running remains near 3.
- Next watch: if pending does not fall materially or oldest pending does not advance past ~03:00 UTC, inspect worker claim cadence and trigger intake volume before increasing concurrency again. Track whether empty/model noise rises under concurrency 3. Consider marking/quarantining blocked output artifacts only after confirming consumers are reading files without DB status checks.
