const { Pool } = require('pg'); const pool = new Pool({ user: 'subcult', host: '127.0.0.1', database: 'subcult_ops', password: 'ea29a9d9d91841abfc0054dc393a9878', port: 5433 }); pool.query("SELECT id, mission_id, kind, status, failure_reason, created_at FROM ops_mission_steps WHERE status = 'failed' ORDER BY created_at DESC LIMIT 10;", (err, res) => { if (err) console.error(err); else console.log(res.rows); pool.end(); });
