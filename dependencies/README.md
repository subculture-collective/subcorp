## Dependencies Management

This directory contains external dependencies for the Subcorp project. Each dependency should have its own subdirectory with a README.md file explaining its purpose and version.